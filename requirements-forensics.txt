# Install AFTER requirements.txt, and note that ember is not in here - it needs
# an ordered install that pip can't express. Run scripts/setup_env.py instead of
# installing this file by hand.

# Exact pin. metadata.json records 1.0.0-d05b3499b as the version that produced
# the training features. NOT 0.11.5 (no wheel past 3.11) and NOT 0.9.0 (what
# ember's setup.py asks for) - the resulting feature drift is disclosed in every
# disk report rather than fixed.
lief==1.0.0

pytsk3
libewf-python

volatility3==2.28.0